using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnInfo : MonoBehaviour
{
    // If true, SpawnPlayer will try to use 'spawnPointName'
    public static bool hasPendingSpawn = false;

    // Name of a GameObject in the next scene to use as the spawn point (e.g., "FromDoor1")
    public static string spawnPointName = "";


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
